# Memory App

> 아이가 자라면서 남기는 기록물(그림·글씨·음성·행동)을 보관하는 감성 앱.
> "그 순간은 사소하지만 시간이 지나면 보물 같은 기억"이 컨셉.

## 컨셉 요약

1. 아이 그림 사진을 찍으면 그림 속 캐릭터/물체가 **살아 움직임**
2. 캐릭터는 말하거나 소리를 내고, 그려진 날짜가 "생일"
3. 캐릭터들이 사는 **집**이 있음 (실제 우리집을 모델링)
4. 한 집 안에 모든 기록물이 공존 — 감사인사가 액자가 되고, 아빠 그림이 집에 살게 됨
5. 집은 이사 가능, 같은 앱 사용자끼리 친구가 될 수 있음
6. 집이 속한 **나라**가 있음 (현재 결정: **가상국가** — "기억나라" 같은 이름, 정치·전쟁 이슈 회피)
7. 아이용 SNS처럼 느린 소통 가능 (피드가 아니라 "친구네 집 방문" 메타포)

자세한 컨셉 토론·결정 사항은 git history 참고.

## 현재 상태 (2026-04 기준)

- **단계**: POC (핵심 기술 검증)
- **검증 가설**: "아이 그림 사진 → 자동 리깅 → 살아 움직이는 캐릭터" 파이프라인이 동작하는가?
- **메인 트랙**: Meta Animated Drawings (결정론적 리깅, 무료, 자체 호스팅)
- **비교 트랙**: fal.ai/Kling (생성형, 유료, 일관성 ↓)
- **POC 코드**: `poc/` 디렉토리. 실행 방법은 `poc/README.md`.

POC 미실행 상태 — 사용자 로컬(Docker 데몬 + 8GB RAM)에서 `bash poc/scripts/setup.sh` 후 실제 아이 그림으로 테스트 필요.

## 디렉토리 안내

```
.
├── CLAUDE.md                          # 이 문서 (Claude 세션이 첫 컨텍스트로 읽음)
├── poc/                               # 핵심 기술 POC
│   ├── README.md                      # 실행법
│   ├── docs/
│   │   ├── success_criteria.md        # POC 합격 기준
│   │   └── decision_log.md            # 왜 이런 설계인지
│   ├── docker-compose.yml
│   ├── docker/Dockerfile              # Animated Drawings + TorchServe
│   ├── src/pipeline.py                # CLI 엔트리
│   └── src/tracks/                    # 트랙 A/B 구현
└── yogibo-korea-10x-strategy.md       # 별개 프로젝트 (무관)
```

## 작업 컨벤션

- **개발 브랜치**: `claude/memory-app-concept-qq6Yt` (이 컨셉의 모든 작업은 여기서)
- **커밋 메시지**: 한국어 본문, conventional commits 접두사(`feat:`, `docs:`, `fix:` 등)
- **언어**: 사용자와 한국어로 대화
- **POC 범위**: "사람 그림 → 움직이는 캐릭터"만. 동물·사물·음성·집은 추후 별도 POC.
- **민감 데이터**: 아이 그림 샘플은 절대 git 커밋 금지 (`poc/.gitignore`로 보호됨)

## 다음 결정 대기 중

POC 실행 결과 확인 후 결정:
1. 합격 → 동물·사물 확장 / 수동 보조 리깅 UI / 음성 / 집 렌더링 중 우선순위
2. 부분 합격 → 실패 모드별 보조 UI 우선
3. 실패 → 컨셉 재조정 (예: 오버레이 효과만)

## 새 Claude 세션 시작 시

1. 이 문서를 읽었다면 컨셉·현재상태 파악 완료
2. `poc/README.md`와 `poc/docs/decision_log.md`를 추가로 읽으면 기술 결정 맥락 복구
3. 사용자에게 "POC 실행해보셨어요? 결과 어땠어요?"부터 묻기 (현재 미실행 상태)
